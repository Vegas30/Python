import heapq
from datetime import time

# # d = date(26,12,2024)
# e = date.fromisoformat('')
# print(e)
# date_rus = "26.12.2024"
#
# # arr = [1, 2, 3, 4, 5, 6, 7]
# for i, _ in range(enumerate(arr)):
#     pass
#
#
# # Создаем пустую кучу
# min_heap = []
# # Добавляем элементы в кучу
# heapq.heappush(min_heap, 50)
# heapq.heappush(min_heap, 30)
# heapq.heappush(min_heap, 40)
# heapq.heappush(min_heap, 10)
# heapq.heappush(min_heap, 20)
# # Печатаем минимальный элемент (он всегда на вершине)
# print("Минимум:", min_heap[0])  # Минимум: 10
# # Извлекаем элементы в порядке возрастания
# while min_heap:
#     print("Извлечено:", heapq.heappop(min_heap))


# d = date(2024,12,26)

t = time.now()
print(t)