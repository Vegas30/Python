# Создайте класс Car, который будет представлять автомобиль, состоящий из двух ключевых компонентов:
# двигателя и бензобака. Класс Car будет использовать композицию, включив в себя объекты классов Engine и FuelTank.
# Каждый из этих классов выполняет свою задачу:
# Класс Engine — управляет состоянием двигателя. Двигатель может находиться в одном из двух состояний:
# запущен (is_running = True) или остановлен (is_running = False).
#
# Метод start() запускает двигатель.
# Метод stop() останавливает двигатель.
#
# Класс FuelTank — управляет топливом в бензобаке автомобиля.
# Имеет атрибут fuel_level, который отслеживает текущий уровень топлива, и capacity, задающий максимальную емкость бака.
# Метод refuel(amount) позволяет пополнить бак на указанное количество топлива.
# Метод consume(amount) отвечает за расход топлива, уменьшая уровень топлива на заданное количество,
# если его достаточно для выполнения операции.
# Класс Car — представляет собой автомобиль, состоящий из объектов Engine и FuelTank.
# Car использует их для управления запуском и остановкой, проверкой и пополнением уровня топлива.
# Этот класс должен предоставлять следующие методы:
#
# start() — проверяет, достаточно ли топлива для запуска автомобиля. Если топлива достаточно,
# происходит запуск двигателя и расходуется определенное количество топлива.
# stop() — останавливает двигатель.
# refuel(amount) — пополняет бензобак на указанное количество топлива.
# check_fuel_level() — выводит текущий уровень топлива в баке.
# Цель задачи: Показать применение композиции, где Car включает в себя Engine и FuelTank как компоненты, которые работают вместе для управления автомобилем.
class FuelTank:

class Engine:
    def __in
class Car: